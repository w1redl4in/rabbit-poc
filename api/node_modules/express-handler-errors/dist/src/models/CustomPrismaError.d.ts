export declare class CustomPrismaError extends Error {
    private readonly errors;
    readonly status: number;
    constructor(errors: string[], status?: number);
    getErrorResponse(): {
        code: string;
        status: number;
        errors: string[];
    };
}
