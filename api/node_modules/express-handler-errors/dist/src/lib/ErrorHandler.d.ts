import { Response, NextFunction } from "express";
import { Logger } from "winston";
export declare class ErrorHandler {
    handle(err: Error, res: Response, next: NextFunction, logger?: Logger, ...callbacks: Function[]): void;
}
